---
- name         : Fastd via pkg manager
  hosts        : all
  gather_facts : yes
  sudo         : True
  connection   : "{{ kitchen_connection | default('local') }}"
  vars_files   :
                 - "fastd_pkg_vars.yml"
  roles        :
                 - "fastd"